export class Notification {

id: number;

name: string
lastName: string
text: string

constructor (id: number, name: string, lastName: string, text: string) {
    this.id = id
    this.name = name
    this.lastName = lastName
    this.text = text
}

}
